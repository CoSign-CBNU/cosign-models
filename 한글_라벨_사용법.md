# 한글 라벨 사용 가이드

## 개요

숫자 라벨(1, 2, 3, 4, 5)을 한글 단어로 표시하는 기능이 추가되었습니다.

## 사용 방법

### 1. 라벨 매핑 파일 수정

`word_labels.json` 파일을 열어서 원하는 한글 단어로 수정하세요:

```json
{
  "1": "안녕",
  "2": "감사합니다",
  "3": "도와주세요",
  "4": "괜찮아요",
  "5": "죄송합니다"
}
```

**실제 수어 단어에 맞게 수정하세요!**

### 2. 데이터셋 재생성

라벨을 수정한 후에는 데이터셋을 다시 빌드해야 합니다:

```bash
# 학습 데이터셋 재생성
python build_dataset.py

# 테스트 데이터셋 재생성 (필요시)
python build_test_repr.py
```

### 3. 모델 재학습

새로운 라벨로 모델을 다시 학습시킵니다:

```bash
python train_rf_all.py
```

### 4. 실시간 인식 테스트

이제 실시간 수어 인식을 하면 한글로 표시됩니다:

```bash
python realtime_sign.py
```

## 작동 원리

### `build_dataset.py`

- `word_labels.json` 파일을 읽어서 한글 매핑을 로드합니다
- `label_map.json`에 한글 라벨 정보를 저장합니다
- 폴더 이름은 여전히 숫자(1, 2, 3, 4, 5)를 유지합니다

### `realtime_sign.py`

- `label_map.json`에서 한글 라벨을 읽어옵니다
- 예측 결과를 한글로 화면에 표시합니다

### `train_rf_all.py`, `eval_rf_on_new.py`

- `label_map.json`의 `id2label`을 사용하여 자동으로 한글 라벨로 출력됩니다
- 별도 수정 불필요

## 주의사항

⚠️ **폴더 구조는 변경하지 마세요**

- `data/1/`, `data/2/` 등의 폴더 이름은 그대로 숫자로 유지해야 합니다
- 한글 라벨은 `word_labels.json`에서만 관리합니다

⚠️ **word_labels.json 파일이 없으면**

- 자동으로 숫자 라벨을 사용합니다
- 기존 동작과 동일하게 작동합니다

## 예시 출력

### 이전 (숫자 라벨)

```
✅ DECISION: 1 (0.95)
Pred: 3 (0.88)
```

### 이후 (한글 라벨)

```
✅ DECISION: 안녕 (0.95)
Pred: 도와주세요 (0.88)
```

## 라벨 변경 체크리스트

1. ✅ `word_labels.json` 파일 수정
2. ✅ `python build_dataset.py` 실행
3. ✅ `python build_test_repr.py` 실행 (테스트 데이터 있는 경우)
4. ✅ `python train_rf_all.py` 실행 (모델 재학습)
5. ✅ `python realtime_sign.py` 실행 (테스트)

완료!
